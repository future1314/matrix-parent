<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:rabbit="http://www.springframework.org/schema/rabbit"
       xmlns:util="http://www.springframework.org/schema/util"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
    http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
    http://www.springframework.org/schema/rabbit
    http://www.springframework.org/schema/rabbit/spring-rabbit-1.0.xsd http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util.xsd">

    <description>rabbitmq 连接服务配置</description>

    <!--数据库连接配置-->
    <util:properties id="mq" location="classpath:application.properties"/>

    <!--  <context:property-placeholder location="classpath:rabbitMQ.properties" /> -->

    <!-- 连接配置 -->
    <rabbit:connection-factory id="connectionFactory"
                               host="#{mq['host']}"
                               username="#{mq['username']}"
                               password="#{mq['password']}"
                               port="#{mq['port']}"
                               channel-cache-size="10"/>

    <rabbit:admin connection-factory="connectionFactory"/>

    <!-- spring template声明-->
    <rabbit:template
            exchange="openapi.tag.delete.exchange.fanout"
            id="amqpTemplate"
            connection-factory="connectionFactory"
            message-converter="jsonMessageConverter"/>

    <!-- 消息对象json转换类 -->
    <bean id="jsonMessageConverter" class="org.springframework.amqp.support.converter.Jackson2JsonMessageConverter"/>


    <rabbit:queue id="test_queue" name="test_queue" durable="true" auto-delete="false" exclusive="false"/>

    <!-- 声明一个Exchange -->
    <rabbit:fanout-exchange name="openapi.tag.delete.exchange.fanout" durable="true" auto-delete="false"
                            id="openapi.tag.delete.exchange.fanout">
        <rabbit:bindings>
            <rabbit:binding queue="test_queue"/>
        </rabbit:bindings>
    </rabbit:fanout-exchange>

</beans>